# Foto/vid user jangan masuk repo (di-upload ulang manual di hosting)
# Struktur folder tetap ikut via .gitkeep
/uploads/*
!/uploads/.gitkeep
/uploads/avatars/*
!/uploads/avatars/.gitkeep

# Dump database lokal: berisi data user, jangan masuk repo publik
/db-backup-lokal.sql
